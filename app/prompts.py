"""
提示词管理模块
使用Python文件存储提示词，便于编辑和管理
"""

# 提示词配置字典
prompts_config = {
    "features": {
        "商品主图": """
    你将收到 5 张图片：
    - images[0]: 基底编辑图（四件套电商图，白底，布局为：左上纸巾、右上叉子、左下6寸盘、右下9寸盘）
    这是唯一的 canvas，最终输出必须与它构图、比例、光影、清晰度、背景完全一致
    - images[1]: 纸巾花纹（必须原样使用）
    - images[2]: 6寸餐盘花纹（必须原样使用）
    - images[3]: 9寸餐盘花纹（必须原样使用）
    - images[4]: 新叉子图片（用于替换 images[0] 中右上角的叉子外观）
    任务类型：【精确贴图替换 + 实物替换（叉子）】，不是重新设计，不是风格参考。严格执行以下操作：
    1)将 images[1] 的图案【不裁剪、不拉伸、不旋转、不改颜色、不改文字】，原样贴到 images[0] 左上角“纸巾”的表面印刷区域。
    2)将 images[2] 的图案【完全原样】贴到 images[0] 左下角“6寸餐盘”的正面印刷区域。
    3)将 images[3] 的图案【完全原样】贴到 images[0] 右下角“9寸餐盘”的正面印刷区域。
    4)将 images[0] 右上角原有叉子替换为 images[4] 中的叉子：- 数量保持一致 - 尺寸、摆放角度、位置与原编辑图完全一致 - 不改变背景、不增加阴影、不改变光源
    绝对禁止的行为（非常重要）：- 不得重绘图案 - 不得重新设计 - 不得“参考后生成相似图” - 不得修改任何文字内容 - 不得改变物体形状、比例、数量、位置 - 不得改变白色背景、整体构图或清晰度
    允许的唯一变化：
    物体表面的印刷图案替换
    右上角叉子实物替换
    输出要求：- 一张 1:1 比例 - 纯白背景 - 亚马逊电商风格产品图 - 与 images[0] 完全同构，只完成指定替换
        """,
        "商品展示图1": "请根据提供的三张图像生成一张展示商品多角度细节的图片",
        "商品尺寸图": "请根据提供的三张图像生成一张清晰展示商品尺寸比例的图片",
        "商品展示图2": "请根据提供的三张图像生成一张商品在不同使用场景中的展示图片",
        "场景展示图1": "请根据提供的三张图像生成一张商品与环境融合的场景展示图片",
        "场景展示图2": "请根据提供的三张图像生成一张突出商品在特定场景中使用效果的图片"
    },
    "default": "请根据提供的三张图像生成一个新的组合图像"
}

def get_prompt(feature: str) -> str:
    """
    根据功能名称获取对应的提示词
    
    Args:
        feature: 功能名称
        
    Returns:
        对应的提示词
    """
    features_prompts = prompts_config.get("features", {})
    default_prompt = prompts_config.get("default", "")
    return features_prompts.get(feature, default_prompt)